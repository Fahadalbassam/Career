# IMPLEMENTATION_TRACKER

Single source of truth for what has been done, what is in progress, and what is queued.

Statuses: `done` · `in-progress` · `pending` · `blocked` · `cancelled`.

| ID | Phase | Task | Status | Files Changed | Why | Tests | Notes | Date |
|---|---|---|---|---|---|---|---|---|
| T-001 | 0 | Repo audit completed | done | — (read-only) | Baseline understanding of frontend and backend before any wiring | manual code review only | Covered `frontend/src/app/`, `components/chat/`, `components/career/`, `components/recommendations/`, `backend/app/` | 2026-05-26 |
| T-002 | — | Backend `/recommend` response upgraded with new fields | done | `backend/app/schemas.py`, `backend/app/recommender.py` | `match_score` (0–100), `score_breakdown`, `role_cluster`, `interview_required`, `missing_skills` are now part of every recommendation | `tests/test_recommender.py` extended | Legacy `score` retained as `match_score / 100` for backward compatibility | 2026-05-27 |
| T-003 | — | Shared rubric module created | done | `backend/app/rubric.py` (new), `backend/app/recommender.py`, `backend/app/build_regression_dataset.py` | Single source of truth for component scorers used by both the live recommender and the regression dataset builder | `tests/test_scoring.py`, `tests/test_recommender.py`, `tests/test_regression_dataset.py` reference the rubric | Weights: 0.35 / 0.20 / 0.15 / 0.10 / 0.10 / 0.05 / 0.03 / 0.02 | 2026-05-27 |
| T-004 | — | `/recommend` exposes match_score / score_breakdown / role_cluster / interview_required / missing_skills | done | `backend/app/recommender.py`, `backend/app/schemas.py::Opportunity` | Required for the new Fit Shelf and confidence copy | `tests/test_recommender.py` | Each field documented in the `Opportunity` Pydantic model | 2026-05-27 |
| T-005 | 0 | Frontend/backend integration audit completed | done | `docs/audits/chat-search-recommendation-audit.md` | Identified mock-vs-real surfaces, animation fragility, missing CORS, type drift risks | manual review | Drove Phases 1–6 in `ROADMAP.md` | 2026-05-27 |
| T-006 | 0 | Planning / tracking docs created | done | `docs/planning/PROJECT_CONTEXT.md`, `docs/planning/ROADMAP.md`, `docs/planning/FRONTEND_BACKEND_INTEGRATION_PLAN.md`, `docs/planning/SEARCH_PAGE_PLAN.md`, `docs/planning/PROMPT_CONTEXT.md`, `docs/tracking/IMPLEMENTATION_TRACKER.md`, `docs/tracking/CHANGE_LOG.md`, `docs/tracking/DECISION_LOG.md`, `docs/tracking/TEST_LOG.md` | Phase 0 deliverable | manual review | This row | 2026-05-27 |
| T-007 | 1 | Add CORS middleware to backend | done | `backend/app/main.py` | Browser-side `fetch` from `http://localhost:3000` is silently blocked without CORS. Must land before any frontend wiring. | manual: `OPTIONS http://localhost:8000/recommend` returns CORS headers; existing `pytest` still passes | `allow_origins` includes `http://localhost:3000` and `http://127.0.0.1:3000` | 2026-05-27 |
| T-008 | 1 | Create frontend API client | done | `frontend/src/lib/api.ts` (new), `frontend/src/lib/api-types.ts` (new), `frontend/.env.local` (new) | One place for `fetch` to the FastAPI backend. Phase 1 exports `recommendFromMessage` only. | manual: `recommendFromMessage("CS student in Riyadh COOP")` returns non-empty `recommendations` | `NEXT_PUBLIC_API_BASE` defaults to `http://localhost:8000` | 2026-05-27 |
| T-009 | 1 | Connect chat to backend (no visual change) | done | `frontend/src/components/chat/career-chat.tsx` | First end-to-end network round-trip from `/chat` | manual: send a chat message; browser console shows `[CareerFinder.ai] /recommend response` | Logs backend JSON only; mock/demo UI flow unchanged. Real card rendering is Phase 2. | 2026-05-27 |
| T-010 | 2 | Map backend recommendation response to frontend types | done | `frontend/src/lib/api-adapters.ts` (new), `frontend/src/lib/types.ts` | snake_case → camelCase; join `why_recommended` list with `" • "`; produce stable `fitLevel` band labels | `npm run lint` | Real `/recommend` data converted to `Recommendation` / `StudentProfile`; mock data unchanged. Side Fit Shelf remains Phase 3; follow-up questions Phase 4. Backend startup verified on port 8000 (`python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000`); port 8001 fallback via `NEXT_PUBLIC_API_BASE` if needed | 2026-05-27 |
| T-011 | 2 | Render real parsed profile in `ParsedProfileCard` | done | `frontend/src/components/chat/career-chat.tsx`, `frontend/src/lib/api-adapters.ts` | Show backend-parsed core fields compactly after assistant replies | manual: send chat message; compact parsed profile card appears when backend succeeds | Core fields only (major, city, interest, work mode, program type, skills). Optional rows (`university`, `preferred_locations`, etc.) remain a small follow-up | 2026-05-27 |
| T-012 | 3 | Render real Fit Shelf cards | done | `frontend/src/lib/api-adapters.ts`, `frontend/src/lib/types.ts`, `frontend/src/components/chat/career-chat.tsx`, `frontend/src/components/chat/fit-shelf-widget.tsx`, `frontend/src/components/chat/fit-shelf.tsx`, `frontend/src/components/chat/fit-shelf-layout.ts` | Replace demo six with real top-K from `/recommend` after successful response; add city / work_mode / role_cluster / interview / missing_skills / source link to the **back** of the widget | `npm run lint`; manual: `/chat` with backend running | Side shelves use real backend recommendations after successful `/recommend`. Demo shelves remain fallback before backend response or if backend fails. Progressive reveal by profile field count (0→6 cards). Follow-up questions remain Phase 4. Multi-turn memory remains Phase 5. Front layout / animation unchanged | 2026-05-27 |
| T-016 | 3.1 | Phase 3.1 Fit Shelf hardening | done | `frontend/src/lib/types.ts`, `frontend/src/lib/api-adapters.ts`, `frontend/src/components/chat/fit-shelf-layout.ts`, `frontend/src/components/chat/career-chat.tsx` | Three correctness fixes: (1) backend shelf detection now uses `source === "backend"` instead of `rank != null` so mock recommendations can never suppress demo tiles; (2) `runAssistantTurn` captures turn id before the fetch starts and guards the async closure so stale/post-clean-slate responses are silently dropped; (3) progressive reveal now counts `interest` + `preferredRoles` and floors at 1 when the backend returns results | `npm run lint`; `npm run build` | `source?: "backend" \| "demo" \| "manual"` added to `CareerFitMemory`. `toCareerFitMemory` sets `"backend"`. `recommendationToMemory` (manual finalize) sets `"manual"`. Demo cards remain undefined. Shelf front / flip / tilt / 3-row behavior unchanged. | 2026-05-27 |
| T-017 | 3.2 | Phase 3.2 Chat layout and response copy hardening | done | `frontend/src/components/chat/career-chat.tsx`, `frontend/src/components/chat/parsed-profile-card.tsx`, `frontend/src/app/(career)/layout.tsx` | Dynamic assistant replies based on missing profile fields and match quality; ParsedProfileCard no longer says "mock"; chat scroll area now fills full viewport height with correct bottom clearance for the fixed composer | `npm run lint`; `npm run build` | `buildAssistantReply` helper produces context-specific replies (soft-skills check, missing major/city/skills prompts, match-score tiering). Layout `pt-4 pb-10` removed; CareerChat outer `pb-48` removed; message content `pb-40` provides composer clearance. No UI redesign. | 2026-05-27 |
| T-019 | Auth-0 | Auth audit and route policy documented | done | `docs/planning/AUTH_PLAN.md`, `docs/tracking/DECISION_LOG.md` (D-011–D-014) | Locks auth architecture before any security surface: HttpOnly sessions, route matrix, import flow, phased plan | manual review | No application auth code in Auth-0 | 2026-05-28 |
| T-020 | Auth-1 | Minimal auth pages and client state (stubbed) | done | `frontend/src/lib/auth.ts`, `frontend/src/components/auth/auth-provider.tsx`, `frontend/src/components/providers.tsx`, `frontend/src/app/login/page.tsx`, `frontend/src/app/signup/page.tsx`, `frontend/src/components/layout/navbar.tsx`, `docs/tracking/*` | Frontend auth shell for demo and API shape before Auth-2 backend | `npm run lint`; `npm run build` | Stub only: in-memory user, no cookies, no middleware, `LoginCardDialog` untouched | 2026-05-28 |
| T-018 | 4A | Phase 4A — Multi-turn profile memory and anonymous local persistence | done | `frontend/src/lib/chat-session.ts` (new), `frontend/src/lib/api-adapters.ts`, `frontend/src/components/chat/career-chat.tsx`, `backend/app/parser.py`, `docs/tracking/*` | Chat was re-parsing only the latest message, causing it to forget fields from earlier turns. Anonymous users now persist their session profile to `localStorage`; clean slate clears it | `npm run lint`; `npm run build`; `pytest tests/test_parser.py`; `pytest tests/test_recommender.py` | No UI redesign. No cookies. No database writes. No auth. No geolocation. Mock fallback retained. | 2026-05-28 |
| T-013 | 4 | Add missing-info follow-up questions | pending | `frontend/src/components/chat/career-chat.tsx`, `frontend/src/lib/confidence.ts` (new) | When required field is missing → ask one focused question instead of returning a weak ranking; when `match_score < 60` → explain what is missing | manual: scripted conversation per `ROADMAP.md` Phase 4 | One question per turn. Chat never ends. | — |
| T-014 | 5 | Add multi-turn profile memory | pending | `frontend/src/components/chat/career-chat.tsx`, `frontend/src/lib/api-adapters.ts`, `frontend/src/lib/intent.ts` (new), `frontend/src/components/career/career-nav-context.tsx` | Profile fields from earlier turns are preserved; refinement intents ("no interview", "remote only") patch the accumulated profile and re-rank | unit tests for `mergeParsedProfiles` and intent detector | Expose `lastChatProfile` on the chrome context for `/search` | — |
| T-015 | 6 | Build search results page | pending | `backend/app/main.py`, `backend/app/schemas.py`, `backend/app/search.py` (new) **or** `backend/app/recommender.py`, `frontend/src/lib/api.ts`, `frontend/src/data/search-filter-options.ts`, `frontend/src/components/career/search-composer.tsx`, `frontend/src/app/(career)/search/page.tsx` | `/search` becomes a real manual filter over the opportunity database with `keyword, company, city, major, role_cluster, program_type, work_mode, skill, interview_required, min_match_score, verified_only` | backend tests for `/opportunities/search`; manual filter-by-filter walkthrough | No user accounts, ratings, recruiter UI, or salary filters | — |
| T-021 | ML-Terminal | Terminal recommender runner (`run:terminal`, `run:full`) | done | `scripts/careerfinder_cli.py`, `scripts/run-terminal.ps1`, `scripts/run-full.ps1`, `scripts/run-terminal.cmd`, `scripts/run-full.cmd`, `package.json` (repo root) | Developer shortcuts to exercise real `/recommend` from the terminal and launch full local stack without touching the web UI | manual: `npm run run:terminal` (CLI + auto backend); `npm run run:full` (backend + frontend windows); piped CLI smoke test | CLI uses `POST /recommend` with accumulated messages; login is stub only; no auth/database/UI changes | 2026-05-28 |
| T-022 | ML-Terminal-UX | Compact terminal recommender output | done | `scripts/careerfinder_cli.py`, `docs/tracking/*` | Default compact profile + one-line top-N table; `/details`, `/open`, `/links`, `/compact`, `/verbose`, `/split`; terminal width via `shutil` | manual: compact turn → `/details 1` → `/open 1` → `/verbose` → `/compact`; `npm run lint`; `npm run build` | Still backend-powered through `/recommend`; no UI/auth/scoring changes | 2026-05-28 |
| T-023 | ML-2A | Role taxonomy, city/interest/skill aliases, parser override, rubric cluster scoring | done | `backend/app/taxonomy.py` (new), `backend/app/parser.py`, `backend/app/rubric.py`, `tests/test_parser.py`, `tests/test_recommender.py`, `docs/tracking/*` | "security focused" / "alkhobar" inputs now parse to `interest=Cybersecurity` / `city=Khobar`; explicit interest overrides major default; cybersecurity interest no longer over-boosts on plain software listings | `pytest tests/test_parser.py` (67/67), `pytest tests/test_recommender.py` (34/34), `pytest tests/test_regression_dataset.py` (9/9), `pytest tests/test_scoring.py` (39/39); manual CLI smoke test with "I am a CS student in alkhobar looking for security focused opportunities" then "I know Linux, networking, Docker, and penetration testing" | No frontend/UI/auth/search/db changes. No model training. New taxonomy module is pure data + small helpers and is imported by both `app.parser` and `app.rubric`. Cluster fallback seeds `preferred_roles` when an interest is detected but no specific job title is named. Response contract unchanged (no new required fields). | 2026-05-28 |
| T-024 | ML-2B | Practical recommender hardening for course-demo quality | done | `backend/app/opportunity_enrichment.py` (new), `backend/app/rubric.py`, `scripts/careerfinder_cli.py`, `tests/test_parser.py`, `tests/test_recommender.py`, `docs/tracking/*` | Runtime opportunity enrichment (telecom / cyber / bank-fintech / AI-data / software buckets) fills in weak inferred role/skills/interests when the dataset metadata is thin. Rubric `compute_skill_match_score` now layers explicit (weight 1.0) vs inferred (weight 0.5) hits; `compute_role_interest_score` adds a 0.5 inferred-interest fallback before generic-technical. CLI gains accidental-input guard, `/undo`, `/history`, a 1–2 s text loading pattern, fixed-width compact table, ANSI section headers, and a score-change explanation note set | `pytest tests/test_parser.py` (70/70), `pytest tests/test_recommender.py` (39/39), `pytest tests/test_regression_dataset.py` (9/9), `pytest tests/test_scoring.py` (39/39); manual CLI: `N` and `/n` ignored, `/history`, `/undo`, alkhobar / security / SQL / DevOps walkthrough | No frontend/UI/auth/search/db changes. No model training. Rubric weights unchanged — only per-token weighting inside `compute_skill_match_score` and a new partial bump inside `compute_role_interest_score`. Enrichment is *secondary*: explicit `skills_list` / `role_cluster` / title still win. | 2026-05-28 |
| T-025 | ML-2B.1 | Required vs preferred role-skill profile enrichment | done | `backend/app/opportunity_enrichment.py`, `backend/app/rubric.py`, `backend/app/recommender.py`, `backend/app/schemas.py`, `tests/test_recommender.py`, `docs/tracking/*` | Added `ROLE_SKILL_PROFILES` (10 simple role profiles, each with `required` + `preferred` skill lists). `enrich_opportunity_signals` now returns `required_skills` and `preferred_skills`. Rubric `compute_skill_match_score` uses 0.7 for inferred-required hits and 0.4 for inferred-preferred hits (explicit still 1.0; generic inferred 0.5). `compute_missing_skills` reorders gaps as required → preferred → remaining explicit and caps at 8. New optional schema fields `missing_required_skills` / `missing_preferred_skills` are populated for callers that want the split | `pytest tests/test_parser.py` (70/70), `pytest tests/test_recommender.py` (48/48), `pytest tests/test_regression_dataset.py` (9/9), `pytest tests/test_scoring.py` (39/39); manual CLI: Cisco NetVersity 70→79 after `Linux + networking`, Help AG Cybersecurity Intern surfaces at #2 after `penetration testing`, Deloitte/Help AG `/details` shows required (`security fundamentals, cybersecurity fundamentals`) before preferred (`firewall, siem, incident response, network monitoring, soc, ...`) | No frontend/UI/auth/search/db changes. No model training. `TARGET_WEIGHTS` byte-identical. New schema fields default to empty list, so existing TypeScript adapter is unaffected (api-types not updated by design). | 2026-05-28 |

|| T-026 | ML-2C | Enriched dataset metadata pass and readable train/test split inspection | done | backend/app/enrich_opportunities_dataset.py (new), backend/app/build_regression_dataset.py, backend/app/inspect_regression_split.py (new), tests/test_regression_dataset.py, docs/tracking/* | Prepare and verify data for the training pipeline: enriched opportunity metadata persisted to CSV, readable train/test split files with profile-ID overlap check | pytest tests/test_regression_dataset.py 16/16 passed; all four suites passed 166+ total | No model training. No frontend/auth/search/db changes. New files: Opportunities_Enriched.csv, regression_train_split.csv, regression_test_split.csv, regression_split_profile_ids.csv, regression_split_summary.json. Split uses GroupShuffleSplit by profile_id; ratio ~80/20; overlapping_profile_ids=[]. | 2026-05-28 |
