import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Search",
}

export default function SearchPage() {
  return (
    <div className="min-h-0 w-full min-w-0 flex-1 basis-0 bg-transparent" />
  )
}
